{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build-all",
			"type": "shell",
			"options": {
				"cwd": "${workspaceFolder}",
			},
			"command": "make ${fileDirname}/${fileBasenameNoExtension}.test",
			"problemMatcher": [
				"cc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
		],
}

